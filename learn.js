//let user = {     // an object
    //name: "John",  // by key "name" store value "John"
    //age: 30        // by key "age" store value 30
  //};
  //alert( user.name ); // John
  //alert( user.age ); // 30
  //console.log(user);
  //user["isAdmin"]=true;
  //console.log(user);
  //let deletedItems=delete user["age"];
  //let deletedItems=delete user.age;
  //console.log("data11 -->" ,deletedItems);
  //console.log(user);
  /*
  let user = {
    name: "John",
    age: 30,
    isAdmin: true
  };
  
  for (let key in user) {
    // keys
    alert( key );  // name, age, isAdmin
    // values for the keys
    alert( user[key] ); // John, 30, true
  }
 */

  //getName();
  //console.log(x);

  var x= 7;

  function getName(){
    console.log("Namaste javaScript");
  }

  // getName();
  console.log(getName);